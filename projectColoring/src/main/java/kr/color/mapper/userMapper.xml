<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.color.mapper.userMapper">

	<insert id="register" parameterType="kr.color.domain.UserInfo">
		insert into
		userInfo(user_id,user_pwd,user_name,user_email,user_phone,admin_yn)
		values(#{user_id},#{user_pwd},#{user_name},#{user_email},#{user_phone},"N")
	</insert>

	<select id="login" parameterType="kr.color.domain.UserInfo"
		resultType="kr.color.domain.UserInfo">
		select * from userInfo where user_id=#{user_id} and
		user_pwd=#{user_pwd};
	</select>

	<insert id="insertMyPalettes"
		parameterType="kr.color.domain.userPalettes">
		INSERT INTO userPalettes (palette_name, palette_color1,
		palette_color2,
		palette_color3, palette_color4, palette_color5,
		regdit_date,
		user_seq)
		VALUES (#{palette_name}, #{palette_color1},
		#{palette_color2}, #{palette_color3}, #{palette_color4},
		#{palette_color5}, #{regdit_date}, #{user_seq},);
	</insert>

</mapper>